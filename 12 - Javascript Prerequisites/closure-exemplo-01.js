var marianaConta = function(quantos) {
    var mensagem = `Mariana conta ${quantos}! Ana, viva Mariana!`;

    var mostrarMensagem = function() {
        console.log(mensagem);
    };

    mostrarMensagem();

};

marianaConta(10);